---
title: learn
slug: learn/
---

<div id="learn-page">

  {{> sidebar}}

  <div class="column-span">

    <section role="region" label="main content">

      <h1>test tutorial title</h1>

      <p>Blah blah blah. Here is some formatted code:</p>

      <pre><code class="language-javascript">   
      var note = "this is formatted code!";
      </code></pre>   

      <p>Blah blah blah. Here is an iframe embedded:</p>

      <iframe src="{{assets}}/learn/test-tutorial/embed.html" width="600px" height="400px"></iframe>

      <p>Blah blah blah. Here is p5-widget embedded:</p>

      <!-- this script only needs to get added once even if there are multiple widget instances -->
      <script src="//toolness.github.io/p5.js-widget/p5-widget.js"></script>
      <script type="text/p5">
function setup() {
  createCanvas(800,800);
}

t2=0;a=3;theta1=0;
function draw() {
  background(200+50*sin(theta1),200+50*sin(theta1+PI/4),200+50*sin(theta1+PI/2));
  strokeWeight(3);
  noFill();
  x0=width/2;
  y0=height/2;  
  theta1=theta1+0.01*PI;
  d2=2;
  delta2=8+cos(2*t2); 
  drawall(x0,y0,d2,delta2,a,theta1);
  
   if(t2>PI*(d2+i*delta2))
  {
    a=-3;    
  }
  if(t2<0)
  {
    a=3;    
  } 
  t2=t2+a;
}

////////
function circle(x0,y0,d2,delta2,i)
{
  stroke(255-cos(t2),150+i*10-0.5*t2,200-cos(t2));
  y0=y0+i*delta2/2;  
  d2=d2+i*delta2;
  r2=d2/2;
  a2=t2/r2;
  x1=x0+t2;
  y1=y0-r2;
  if(t2<=PI*d2)
  {
    line(x0,y0,x1,y0);
    arc(x1,y1,d2,d2,a2-3/2*PI,1/2*PI);  
  }
   else
  {
    line(x0,y0,x0+PI*d2,y0);
  } 
}
function drawcircle(x0,y0,d2,delta2)
{
  i=0;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
  i++;circle(x0,y0,d2,delta2,i);
}
function drawall(x0,y0,d2,delta2,a,theta1)
{
  drawcircle(x0,y0,d2,delta2);
  translate(width, 0);
  rotate(PI/2);
  drawcircle(x0,y0,d2,delta2);
  translate(width, 0);
  rotate(PI/2);  
  drawcircle(x0,y0,d2,delta2); 
  translate(width, 0);
  rotate(PI/2); 
  drawcircle(x0,y0,d2,delta2);  
 
}
</script>

      <p>The end!</p>

    </section>

    {{> footer}}

  </div> <!-- end column-span -->

  {{> asterisk}}

</div><!-- end id="get-started-page"  -->
